# UX / Dashboard / Mock Permission

기존 계산 엔진, Mock/GA4 Connector, 관리자 상세 기능을 유지한 역할별 화면 개편입니다. 로그인·OAuth는 구현하지 않았습니다.

## 검수 방법

`npm run dev` 후 사이드바의 **현재 보기**에서 관리자/광고주를 전환합니다. 기본 검수 역할은 관리자, 광고주 역할은 `brand-a`에 연결됩니다. 관리자가 B를 보고 있어도 광고주 역할로 전환하면 A만 표시합니다. 역할은 탭의 sessionStorage에 유지되고, GA4 Route의 Mock 권한 검수에는 SameSite 쿠키를 함께 사용합니다. 개발 환경에서는 전환 도구를 항상 제공하고, production 검수에서는 관리자로 시작한 미리보기에서 제공합니다. 광고주 역할로 새로고침한 production 화면에는 관리자 전환 도구가 없습니다.

| 기능 | 관리자 | 광고주 |
| --- | --- | --- |
| 대시보드 | 전체 광고주 우선 확인 목록 | 자신의 KPI 4개·핵심 내용·예상 성과·광고 상태·추이 1개 |
| 고객 여정 | 기존 Source·고객 그룹·상세 이벤트 | 5단계·전환율·이탈 수 |
| 성과 개선 | 기존 레버·저장·비교·시나리오 | 보수적·추천·적극적 비교 |
| 캠페인 | 생성·수정·Mock 활성화 | 광고 현황 읽기 전용 |
| 광고 운영 | 규칙·적용·복원·이력 | 접근 차단 |
| 성과 보고서 | 상세 성과·유입·운영·Export | 성과 설명 중심 요약 |
| 데이터 연결 / 관리 설정 | 연결 테스트·설정·백업·Import | 접근 차단 |
| 광고주 관리 | A/B 기본정보·연결 상태·성과·Workspace 이동 | 접근 차단 |

관리자 기존 홈의 상세 성과도 **선택한 광고주 상세 성과 보기**에 보존했습니다. 관리자 대시보드의 전체 광고주 요약은 Mock 운영 데이터를 사용하며 GA4 Real과 합산하지 않습니다. 광고주 GA4 화면은 실제 구매·매출을 사용하고 광고비/ROAS는 미연결로 표시합니다. GA4 이벤트는 순차 코호트가 아니므로 전환율·이탈자를 만들어내지 않습니다. 구매 개선 가정과 모든 기존 예측에는 DEMO FORECAST가 유지됩니다.

## 권한과 서비스 경계

- `types/auth.ts`: SessionUser → role / advertiserId. 이후 실제 세션 Resolver로 교체할 수 있는 계약입니다.
- `lib/permissions.ts`: `can`, `requirePermission`, `requireAdvertiser`, `assertAccess`와 Mock 세션 Adapter.
- `lib/route-permissions.ts`: 메뉴와 Route 권한 매핑. 광고주 메뉴는 5개이며 제한 URL은 안내 후 대시보드로 이동할 수 있습니다.
- `context/user-role-context.tsx`, `components/app-shell.tsx`: 역할 전환, 광고주 고정, URL 광고주 검증, 기존 광고주의 비동기 결과 노출 방지.
- Dashboard/Funnel/GA4/Store 읽기에는 광고주 범위를 검사합니다. 캠페인 저장·복제·상태 변경·운영 쓰기·적용·복원·설정·백업·Import에는 함수 수준의 권한 검사를 적용합니다.
- `loadOperations`는 관리자 상세 서비스입니다. `loadOperationsSummary`는 읽기 전용 계산 후 안전한 요약만 반환하며 규칙/이력을 반환하거나 저장하지 않습니다. 광고주 화면에는 내부 규칙을 전달하지 않습니다.
- `/api/ga4`에서도 광고주 범위와 연결 테스트 권한을 검사합니다. 기존 실제 GA4 조회의 로컬 개발 환경 제한을 유지합니다.

**이 구조는 실제 보안 인증이 아닙니다.** Mock Role의 브라우저 저장소/쿠키와 번들된 Mock 데이터는 사용자가 변경하거나 열람할 수 있습니다. production SaaS 권한 경계에는 신뢰 가능한 서버 세션, 서버 데이터 저장소, 서버에서 조회한 사용자→광고주 매핑 및 매 요청의 정책 검증이 필요합니다. 클라이언트가 전달하는 역할/광고주 ID를 인증 근거로 사용해서는 안 됩니다. 이번 작업은 그 정책의 Route/Action/Service 동작을 검수하는 단계입니다.

## UI 기준

한글 우선 메뉴, 공통 상단 제목/광고주/기간과 데이터 출처 표시, 행과 구분선 중심의 대시보드, Mint 행동 버튼·Violet 예상 성과·Amber 확인 필요를 적용했습니다. 모바일 메뉴는 가로 스크롤이며 광고주 KPI는 2열입니다. 테이블은 자체 스크롤을 사용하고 고급 상세는 관리자 화면이나 접힌 상세 영역에 보존합니다.

테스트 명령: `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`, `npm run test:ui`.

2026-09-17 최종 결과: TypeScript/Lint 성공, 단위 51/51, 브라우저 37/37, production build 성공. 기존 계산·저장·시나리오 전달·운영 적용/복원·GA4 Fixture 회귀를 포함합니다. 광고주 390/768/1024/1366px와 관리자 주요 화면의 가로 넘침을 검증했습니다. 역할 비교는 중앙 permissions 모듈에만 존재하며 Reference HTML의 SHA256은 변경 전과 동일합니다. 실제 외부 API나 로그인 테스트는 수행하지 않았습니다.
