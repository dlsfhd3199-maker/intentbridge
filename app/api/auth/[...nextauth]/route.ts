import type {NextRequest} from 'next/server';import {handlers} from '@/auth';import {withRequest} from '@/lib/server/request-context';import {enforceRate,clientAddress} from '@/lib/server/request-protection';import {apiError,AccessError} from '@/lib/server/authorization';export const runtime='nodejs';
export async function GET(r:NextRequest){return withRequest(r,()=>handlers.GET(r))}
export async function POST(r:NextRequest){return withRequest(r,async()=>{try{if(process.env.AUTH_URL&&r.headers.get('origin')&&r.headers.get('origin')!==new URL(process.env.AUTH_URL).origin)throw new AccessError(403);if(r.nextUrl.pathname.includes('/signin/')){if(Number(r.headers.get('content-length'))>4096)throw new AccessError(413);await enforceRate('loginIp',clientAddress(r));const form=await r.clone().formData();const email=String(form.get('email')??'').trim().toLowerCase();if(email.length>254)throw new AccessError(400);await enforceRate('loginEmail',email);}return handlers.POST(r)}catch(e){return apiError(e)}})}
