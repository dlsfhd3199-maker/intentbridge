import type { GA4ErrorCode,GA4Failure } from "../../types/ga4";
const messages:Record<GA4ErrorCode,string>={MISSING_CONFIGURATION:"GA4 Property ID와 서버 Service Account 설정이 필요합니다.",INVALID_PROPERTY:"GA4 Property ID 또는 조회 설정을 확인하세요.",PERMISSION_DENIED:"GA4 Property 조회 권한 또는 Data API 활성화 상태를 확인하세요.",AUTHENTICATION_ERROR:"서버 Service Account 인증 설정을 확인하세요.",QUOTA_ERROR:"GA4 할당량이 소진되었습니다. 잠시 후 다시 확인하세요.",RATE_LIMIT:"GA4 요청 한도에 도달했습니다. 잠시 후 다시 확인하세요.",API_ERROR:"GA4 데이터를 불러오지 못했습니다. 잠시 후 다시 시도하세요.",TIMEOUT:"GA4 응답 시간이 초과되었습니다. 잠시 후 다시 시도하세요."};
export class GA4Error extends Error {constructor(public code:GA4ErrorCode){super(messages[code]);}}
export function safeGA4Error(error:unknown):GA4Failure {let code:GA4ErrorCode="API_ERROR";if(error instanceof GA4Error)code=error.code;else if(error&&typeof error==="object"){const raw=error as {code?:unknown;message?:unknown};const n=Number(raw.code);if([7,403].includes(n))code="PERMISSION_DENIED";else if([16,401].includes(n))code="AUTHENTICATION_ERROR";else if([3,5,400,404].includes(n))code="INVALID_PROPERTY";else if([4,408,504].includes(n))code="TIMEOUT";else if([8,429].includes(n))code=typeof raw.message==="string"&&/quota|exhausted/i.test(raw.message)?"QUOTA_ERROR":"RATE_LIMIT";}return {code,message:messages[code]};}
