"use client";
import {useEffect,useState} from "react";
export function SystemReadiness(){const [data,setData]=useState<Record<string,string>>({}),[error,setError]=useState("");useEffect(()=>{fetch("/api/admin/readiness",{cache:"no-store"}).then(async r=>{if(!r.ok)throw new Error();setData(await r.json())}).catch(()=>setError("시스템 상태를 확인하지 못했습니다."))},[]);return <section><h2>System Readiness <small>{data.environment}</small></h2><p>외부 API 연결 테스트 없이 구성 여부를 표시합니다.</p><dl>{Object.entries(data).filter(([key])=>key!=="environment").map(([key,value])=><div key={key}><dt>{key}</dt><dd>{value.replaceAll("MOCK","DEMO")}</dd></div>)}</dl><p>광고 매체 · Naver / Meta / Google / Kakao / Daangn: DEMO CONNECTION</p>{error&&<p role="alert">{error}</p>}</section>}
