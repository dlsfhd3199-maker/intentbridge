"use client";
import {useEffect,useState} from "react";
import type {Query} from "@/types/domain";
import type {OperationsWorkspace} from "@/types/operations";
import {loadOperations} from "@/lib/operations-engine";
import {readCampaignStore} from "@/lib/campaign-store";
export function MockReportSummary({query}:{query:Query}){const [data,setData]=useState<OperationsWorkspace|null>(null),[error,setError]=useState(false);useEffect(()=>{let active=true;setData(null);setError(false);loadOperations(query.advertiserId,query.period).then(r=>{if(active)setData(r)}).catch(()=>{if(active)setError(true)});return()=>{active=false};},[query.advertiserId,query.period]);if(error)return <p role="alert">Mock 운영 요약을 불러오지 못했습니다. Operations에서 다시 확인하세요.</p>;if(!data)return <p role="status">Mock 운영 요약 로딩…</p>;const counts={Draft:readCampaignStore(query.advertiserId).drafts.length,Ready:data.campaigns.filter(r=>r.campaign.status==="READY").length,"Mock Active":data.campaigns.filter(r=>r.campaign.status==="MOCK ACTIVE").length,"Action Required":data.campaigns.filter(r=>r.health==="ACTION REQUIRED").length};return <section><h2>Campaign / Operations · MOCK AD DATA</h2><p>GA4 구매에 포함 여부를 매칭한 데이터가 아닙니다. 아래 수치는 로컬 Mock 캠페인만 반영합니다.</p><div className="mvp-inline-counts">{Object.entries(counts).map(([k,v])=><span key={k}>{k} <b>{v}</b></span>)}</div>{data.campaigns.slice(0,3).map(r=><p key={r.campaign.id}>{r.campaign.name} · {r.campaign.status} · {r.health}</p>)}{!data.campaigns.length&&<p>아직 Mock 캠페인이 없습니다.</p>}<h3>최근 Mock 변경</h3>{data.store.history.slice(0,3).map(h=><p key={h.id}>{h.campaignName} · {h.type} · {h.status}</p>)}<h3>검토 대기 추천 · MOCK AUTOMATION</h3>{data.recommendations.filter(r=>r.status==="PENDING").slice(0,3).map(r=><p key={r.id}>{r.campaignName} · {r.rule.action.type}</p>)}</section>;}
